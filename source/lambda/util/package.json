{
  "name": "utils",
  "version": "1.0.1",
  "author": "AWS Solutions",
  "license": "Apache-2.0",
  "description": "Utilities to be used in the solution's Lambda packages",
  "main": "metrics.js",
  "scripts": {
    "clean": "rm -rf node_modules/ dist/ coverage/ package-lock.json",
    "test": "jest --config jestconfig.json --coverage --silent"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.25",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "moment": "^2.29.1"
  }
}
